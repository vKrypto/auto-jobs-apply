version: '3.8'

services:
  backend:
    build: .
    ports:
      - "5000:5000"
    environment:
      - DB_TYPE=mock
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - DEBUG=false
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped

